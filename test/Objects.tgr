#ifndef OBJECTS_TGR
#define OBJECTS_TGR

#include "Shape.tgr"

Object = struct {
    astring     name
    opt wstring creator
    bool        visible
    Shape       shape
}

Objects = array(Object object)

ListOfNames = array(wstring name)

#endif
