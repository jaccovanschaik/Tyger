#include "Shape.tgr"

Object = struct {
    astring     name
    opt ustring creator
    bool        visible
    Shape       shape
}

Objects = array(Object object)

ListOfNames = array(ustring name)
